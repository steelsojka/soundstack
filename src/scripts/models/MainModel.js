define(["components/MediaStream", "models/StackCollection"], function(MediaStream, StackCollection) {

  var MainModel = Backbone.Model.extend({
    initialize : function() {
    
    }
  });

  return MainModel;

});